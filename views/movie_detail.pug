extends layout

block content
  h1 #{movie.title}

  if movie.year
    p Year: #{movie.year}
  else
    p Year: Unknown
  if movie.tagling
    p Tagline: #{movie.tagline}
  else
    p Tagline: Unknown
  if movie.overview
    p Overview: #{movie.overview}
  else
    p Overview: Unknown
